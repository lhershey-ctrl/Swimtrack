---
name: user-liron
description: Who the user is and their working style on the SwimTrack project
metadata: 
  node_type: memory
  type: user
  originSessionId: aa2468ed-d83c-497d-a96c-a5606287bdf8
---

Liron (GitHub `lhershey-ctrl`, Google `lhershey@gmail.com`). Builds personal apps to track his kids' competitive swimming (Noga, Gal) and his own (Liron) using data from the Israeli swim site **loglig.com**. Has several Firebase projects already (dog-walker, t1d-hub, travel-tracker, arcade-app) and is comfortable deploying with the Firebase CLI (already logged in) and Vite/React.

Works iteratively in small steps, gives rapid feedback, and expects each change deployed + pushed promptly. Prefers cloud-first (edit anywhere, see on phone) over file shuffling. Family member `sharos88@gmail.com` (Sharon) also uses the apps.

Works across at least two machines (previously `c:\Users\liron.hs\...`, now this machine at `d:\Liron\Tools\Swimtrack`) — memory is mirrored into the repo's own `memory/` folder (git-tracked) specifically so context survives machine switches. See [[swimtrack-references]].
